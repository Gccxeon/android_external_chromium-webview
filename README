This is an alternative version of "platform/external/chromium-webview/" where
the compiled WebView is patched for compatibility with older Broadcom GPUs.

The patches can be found in the "patches" directory of this repository.

The prebuilt libwebviewchromium.so included in these APKs is built from Chromium
release tag 57.0.2939.0

args.gn
```
target_os="android"
target_cpu="arm"
is_debug=false
is_official_build=true
is_chrome_branded=false
use_official_google_api_keys=false
exclude_unwind_tables=true
enable_resource_whitelist_generation=true
ffmpeg_branding="Chrome"
proprietary_codecs=true
```
